{
  "name": "lms-portal",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development DB_PATH=./data/app.db DEMO_MODE=true node server.js",
    "build": "vite build",
    "start": "node server.js",
    "deploy:azure": "npm run build && npm run zip && az webapp deploy --name app-lms-portal --resource-group Dima-Gibert-RG --src-path deploy.zip --type zip",
    "zip": "zip -r deploy.zip dist/ public/ server.js package.json package-lock.json node_modules/"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "@hono/node-server": "^2.1.1",
    "better-sqlite3": "^13.0.3",
    "hono": "^4.13.5"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.11.1",
    "@types/better-sqlite3": "^9.6.0",
    "@types/node": "^22.20.1",
    "typescript": "^7.0.2",
    "vite": "^8.1.4"
  }
}
